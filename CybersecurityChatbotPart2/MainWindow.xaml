<Window x:Class="CybersecurityChatbotPart2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Orbit Security System" Height="600" Width="450"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- ASCII Art -->
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Chat History -->
            <RowDefinition Height="Auto"/> <!-- Input Panel -->
            <RowDefinition Height="Auto"/> <!-- Status Bar -->
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1E2A36" Padding="10">
            <TextBlock x:Name="AsciiArtBlock" FontFamily="Consolas" Foreground="#4CAF50" HorizontalAlignment="Center" TextAlignment="Center"/>
        </Border>

        <ListBox x:Name="ChatHistoryBox" Grid.Row="2" Background="#F8F9F9" Margin="5"
                 ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="5" FontSize="14" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="3" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBox x:Name="InputTextBox" Grid.Row="0" Margin="0,0,0,5" Padding="5" FontSize="14" VerticalContentAlignment="Center" KeyDown="InputTextBox_KeyDown" Height="40"/>
            <UniformGrid Columns="5" Grid.Row="1" Margin="0,0,0,10">
                <Button x:Name="BtnSend" Content="Send" Background="#3498DB" Foreground="White" FontWeight="Bold" Click="SendButton_Click" Height="40"/>
                <Button x:Name="BtnTasks" Content="Tasks" Margin="5,0" Background="#2ECC71" Foreground="White" FontWeight="Bold" Click="BtnTasks_Click" Height="40"/>
                <Button x:Name="BtnQuiz" Content="Quiz" Margin="5,0" Background="#F39C12" Foreground="White" FontWeight="Bold" Click="BtnQuiz_Click" Height="40"/>
                <Button x:Name="BtnLog" Content="Log" Margin="5,0" Background="#9B59B6" Foreground="White" FontWeight="Bold" Click="BtnLog_Click" Height="40"/>
                <Button x:Name="BtnClearMemory" Content="Clear Memory" Margin="5,0,0,0" Background="#E67E22" Foreground="White" FontWeight="Bold" Click="ClearMemoryButton_Click" Height="40"/>
            </UniformGrid>
        </Grid>

        <Border Grid.Row="4" Background="#EFEFEF" Padding="5">
            <TextBlock Text="Try asking about: phishing, passwords, malware, social engineering, privacy" FontSize="12" Foreground="#666666" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
