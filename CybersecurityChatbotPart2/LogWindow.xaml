<Window x:Class="CybersecurityChatbotPart2.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Activity Log" Height="400" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Recent Activity" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
        <ListBox x:Name="LstLog" Grid.Row="1" Margin="0,0,0,10"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnRefresh" Content="Refresh" Width="80" Click="BtnRefresh_Click"/>
            <Button x:Name="BtnClear" Content="Clear" Width="80" Margin="5,0" Click="BtnClear_Click"/>
        </StackPanel>
    </Grid>
</Window>
